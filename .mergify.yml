pull_request_rules:
  - name: h1lab native-scope zero-review merge
    conditions:
      - base = main
      - -scope = critical
    actions:
      merge:
        method: squash

scopes:
  source:
    files:
      critical:
        include:
          - critical/**
