commands_restrictions:
  queue:
    conditions:
      - sender = onecursor5-design

queue_rules:
  - name: h1-dq-1786469625270-0aaf5f
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = @github-actions/H1 dequeue gate
    branch_protection_injection_mode: merge
    batch_size: 1
    merge_method: squash

merge_queue:
  mode: serial
  max_parallel_checks: 2
  skip_intermediate_results: false
  queue_controls_comment: false
