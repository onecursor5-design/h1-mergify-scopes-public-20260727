pull_request_rules:
  - name: native-files-old-path-protected-merge
    conditions:
      - base = main
      - -files ~= ^critical/
      - "#approved-reviews-by >= 1"
    actions:
      merge:
        method: squash
