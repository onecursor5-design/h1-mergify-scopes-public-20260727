commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write

queue_rules:
  - name: h1-ready-1786477366812-ecf280
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = @github-actions/H1 ready head timer 1786477366812-ecf280
    branch_protection_injection_mode: merge
    batch_size: 1
    checks_timeout: 5 minutes
    merge_method: merge

merge_queue:
  mode: serial
  max_parallel_checks: 2
  skip_intermediate_results: false
  queue_controls_comment: false
