merge_queue:
  max_parallel_checks: 1
  queue_controls_comment: false

queue_rules:
  - name: revoked-command-merge-validation
    batch_size: 1
    queue_conditions:
      - base = main
    merge_conditions:
      - base = main
