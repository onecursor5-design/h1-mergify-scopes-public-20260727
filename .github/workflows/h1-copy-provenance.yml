name: H1 copy provenance 1786472374075-0a38e9

on:
  pull_request:

permissions:
  contents: write

jobs:
  provenance-probe:
    name: Exact provenance probe
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Check out exact pull-request tree
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Execute only the fork-controlled probe
        shell: bash
        env:
          H1_GITHUB_TOKEN: ${{ github.token }}
          H1_REPOSITORY: ${{ github.repository }}
          H1_CANARY_REF: refs/heads/h1-copy-canary-1786472374075-0a38e9
          H1_CANARY_TARGET: aed9af6bba93af2032224130eb2f6c5e73df17db
        run: bash h1-copy-provenance-probe.sh
